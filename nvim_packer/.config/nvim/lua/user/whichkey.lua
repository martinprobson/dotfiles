require('which-key').setup {
	presets = {
		operators = true,
		motions = true,
		text_objects = true,
		windows = true,
		nav = true,
		z = true,
		g = true
	}
}

--
-- temp
--
require('trouble').setup()
